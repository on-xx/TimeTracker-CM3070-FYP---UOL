import {StyleSheet} from 'react-native';

const globalStyle = StyleSheet.create({
  flex: {
    flex: 1,
  },
});

export const colors = {
  primary_color: '#EC1E1E',
  boxborder_color: '#8A8A8A',
};

export default globalStyle;
